#!/bin/sh
set -eu
node "/Users/sawbeck/.codex/guardrail-kit/bin/guardrail-pre-commit.js"

repo_root="$(git rev-parse --show-toplevel)"
cd "$repo_root"

node scripts/enforcement/pre-commit.js

# keel proof-gate (fail-open: no-op when keel-core or .keel/verify is absent)
KEEL_CORE="/Users/sawbeck/.keel/bin/keel-core.sh"
[ -f "$KEEL_CORE" ] && { . "$KEEL_CORE"; keel_run_proof || exit $?; }
exit 0
