# Session Handoff

Update `docs/status/` when a meaningful batch ends.

Capture:

- what changed materially
- what the next site batch should be
- the biggest open risk

Do not capture:

- every file touched
- every command run
- low-level implementation noise that will age badly
