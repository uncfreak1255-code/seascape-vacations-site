{% from "partials/ui-icon.njk" import uiIcon %}
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Luxury Vacation Rentals AMI, Bradenton & Sarasota | Seascape</title>
    <meta name="description"
        content="Book direct and save on luxury vacation rentals in Anna Maria Island, Bradenton, and Sarasota Florida. Private pools, waterfront homes, pet-friendly options.">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="website">
    <meta property="og:url" content="https://seascape-vacations.com/">
    <meta property="og:title" content="Seascape Vacations | Luxury Florida Vacation Rentals">
    <meta property="og:description" content="Book direct and save on luxury vacation rentals in Anna Maria Island, Bradenton, and Sarasota Florida. Private pools, waterfront homes, pet-friendly options.">
    <meta property="og:image" content="https://seascape-vacations.com/images/seascape-og-default.jpg">
    
    <!-- Twitter -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="Seascape Vacations | Luxury Florida Vacation Rentals">
    <meta name="twitter:description" content="Book direct and save on luxury vacation rentals in Anna Maria Island, Bradenton, and Sarasota. Private pools, waterfront homes.">
    <meta name="twitter:image" content="https://seascape-vacations.com/images/seascape-og-default.jpg">
    
    <!-- Additional SEO -->
    <meta name="robots" content="index, follow">
    <meta name="author" content="Seascape Vacations">
    <link rel="canonical" href="https://seascape-vacations.com/">
    <link rel="icon" href="/logo-optimized.png" type="image/png">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <!-- DNS Prefetch for third-party resources (improves mobile performance) -->
    <link rel="dns-prefetch" href="https://connect.facebook.net">
    <link rel="dns-prefetch" href="https://www.googletagmanager.com">
    <!-- Preload critical assets - responsive hero images -->
    <link rel="preload" href="hero-mobile.webp" as="image" type="image/webp" fetchpriority="high" media="(max-width: 768px)">
    <link rel="preload" href="hero-optimized.webp" as="image" type="image/webp" fetchpriority="high" media="(min-width: 769px)">
    
    <!-- LocalBusiness Schema -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "LocalBusiness",
        "name": "Seascape Vacations",
        "description": "Luxury vacation rental property management company serving Anna Maria Island, Bradenton, Sarasota, and Siesta Key, Florida.",
        "url": "https://seascape-vacations.com",
        "logo": "https://seascape-vacations.com/logo-optimized.png",
        "image": "https://seascape-vacations.com/images/seascape-og-default.jpg",
        "telephone": "+1-941-704-8545",
        "email": "info@seascape-vacations.com",
        "address": {
            "@type": "PostalAddress",
            "addressLocality": "Bradenton",
            "addressRegion": "FL",
            "addressCountry": "US",
            "postalCode": "34209"
        },
        "areaServed": [
            {"@type": "City", "name": "Anna Maria Island"},
            {"@type": "City", "name": "Bradenton"},
            {"@type": "City", "name": "Sarasota"},
            {"@type": "City", "name": "Siesta Key"},
            {"@type": "City", "name": "Longboat Key"}
        ],
        "priceRange": "$400-$800/night",
        "openingHoursSpecification": {
            "@type": "OpeningHoursSpecification",
            "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday", "Friday", "Saturday", "Sunday"],
            "opens": "00:00",
            "closes": "23:59"
        },
        "sameAs": [
            {% for profile in site.sameAsLinks %}
            "{{ profile }}"{% if not loop.last %},{% endif %}
            {% endfor %}
        ]
    }
    </script>
    <!-- WebSite SearchAction Schema -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "WebSite",
        "name": "Seascape Vacations",
        "url": "https://seascape-vacations.com/",
        "potentialAction": {
            "@type": "SearchAction",
            "target": {
                "@type": "EntryPoint",
                "urlTemplate": "https://seascape-vacations.com/properties/?area={search_term_string}"
            },
            "query-input": "required name=search_term_string"
        }
    }
    </script>
    <!-- Non-blocking Google Fonts (fixes LCP issue) -->
    <link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap" media="print" onload="this.media='all'"
    >
    <noscript>
        <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Playfair+Display:ital,wght@0,400;0,500;0,600;0,700;1,400&display=swap" rel="stylesheet">
    </noscript>

    <!-- Meta Pixel & GA4 - Deferred until after page load for performance -->
    <script>
        // Load tracking scripts after page is interactive (non-blocking)
        window.addEventListener('load', function() {
            // Meta Pixel - load asynchronously after paint
            setTimeout(function() {
                !function(f,b,e,v,n,t,s){
                    if(f.fbq)return;n=f.fbq=function(){n.callMethod?
                    n.callMethod.apply(n,arguments):n.queue.push(arguments)};
                    if(!f._fbq)f._fbq=n;n.push=n;n.loaded=!0;n.version='2.0';
                    n.queue=[];t=b.createElement(e);t.async=!0;
                    t.src=v;s=b.getElementsByTagName(e)[0];
                    s.parentNode.insertBefore(t,s)
                }(window,document,'script','https://connect.facebook.net/en_US/fbevents.js');
                fbq('init','2748551298816267');
                fbq('track','PageView');
            }, 100);
            
            // GA4 - load asynchronously after paint
            setTimeout(function() {
                var s=document.createElement('script');
                s.async=true;s.src='https://www.googletagmanager.com/gtag/js?id=G-3VDV66S3DK';
                document.head.appendChild(s);
                window.dataLayer=window.dataLayer||[];
                function gtag(){dataLayer.push(arguments);}
                gtag('js',new Date());gtag('config','G-3VDV66S3DK');
            }, 200);
        });
    </script>
    <noscript><img height="1" width="1" style="display:none" src="https://www.facebook.com/tr?id=2748551298816267&ev=PageView&noscript=1"/></noscript>
    <!-- End Tracking Scripts -->

    <link rel="stylesheet" href="/css/homepage.css">
    <!-- EmailJS SDK - Deferred for performance -->
    <script>
        // Load EmailJS after page is interactive (non-blocking)
        window.addEventListener('load', function() {
            setTimeout(function() {
                var s = document.createElement('script');
                s.src = 'https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js';
                s.onload = function() {
                    emailjs.init("YOUR_PUBLIC_KEY");
                };
                document.head.appendChild(s);
            }, 500);
        });
    </script>
    <!-- Preconnect to image optimization CDN -->
    <link rel="preconnect" href="https://bookingenginecdn.hostaway.com" crossorigin>
    <link rel="dns-prefetch" href="https://bookingenginecdn.hostaway.com">
</head>

<body>

    <!-- NAV -->
    <nav class="nav" id="mainNav">
        <div class="nav-inner">
            <a class="nav-logo" href="/">
                <svg viewBox="0 0 100 100" width="44" height="44" fill="none" stroke="#5F8A8B" stroke-width="3"
                    stroke-linecap="round" stroke-linejoin="round">
                    <!-- Rounded triangle outline -->
                    <path d="M50 12 L88 78 Q92 86 84 88 L16 88 Q8 86 12 78 Z" />
                    <!-- Crashing wave -->
                    <path d="M20 72 Q32 50 48 56 Q62 62 72 52 Q78 46 82 48" stroke-width="3.5" />
                    <!-- Water spray marks -->
                    <path d="M26 62 Q32 54 38 58" stroke-width="2.5" />
                    <path d="M30 70 Q36 62 42 66" stroke-width="2.5" />
                    <path d="M22 78 Q28 70 34 74" stroke-width="2.5" />
                </svg>
                <div>
                    <div class="nav-logo-text">SEASCAPE</div>
                    <div class="nav-logo-sub">Vacations</div>
                </div>
            </a>
            <div class="nav-links">
                <a class="nav-link" href="/">Home</a>
                <a class="nav-link" href="/properties/">Properties</a>
                <div class="nav-dropdown">
                    <span class="nav-link">Destinations ▾</span>
                    <div class="nav-dropdown-content">
                        <a href="/guides/anna-maria-island-area-guide/">Anna Maria Island</a>
                        <a href="/guides/bradenton-area-guide/">Bradenton</a>
                        <a href="/guides/sarasota-area-guide/">Sarasota</a>
                        <a href="/guides/siesta-key-area-guide/">Siesta Key</a>
                    </div>
                </div>
                <a class="nav-link" href="/about-us/">About Us</a>
                <a class="nav-link" href="/property-management/">Property Owners</a>
                <a class="nav-link" href="/guides/">Guides</a>
                <a class="nav-link" href="#contact">Contact</a>

                <button class="btn" onclick="window.open('https://book.seascape-vacations.com', '_blank')">Book
                    Direct</button>
            </div>
            <button class="mobile-btn" onclick="toggleMenu()" type="button" aria-label="Open menu" aria-expanded="false" aria-controls="mobileMenu">{{ uiIcon('menu', 'ui-icon--mobile-toggle') }}</button>
        </div>
        <div id="mobileMenu" class="mobile-menu">
            <a href="/" class="mobile-item" onclick="toggleMenu()">Home</a>
            <a href="/properties/" class="mobile-item" onclick="toggleMenu()">Properties</a>
            <span class="mobile-item" style="color:var(--brand);font-weight:600;">Destinations</span>
            <a href="/guides/anna-maria-island-area-guide/" class="mobile-item" style="padding-left:24px;font-size:14px;" onclick="toggleMenu()">› Anna Maria
                Island</a>
            <a href="/guides/bradenton-area-guide/" class="mobile-item" style="padding-left:24px;font-size:14px;" onclick="toggleMenu()">›
                Bradenton</a>
            <a href="/guides/sarasota-area-guide/" class="mobile-item" style="padding-left:24px;font-size:14px;" onclick="toggleMenu()">›
                Sarasota</a>
            <a href="/guides/siesta-key-area-guide/" class="mobile-item" style="padding-left:24px;font-size:14px;" onclick="toggleMenu()">› Siesta
                Key</a>
            <a href="/about-us/" class="mobile-item" onclick="toggleMenu()">About Us</a>
            <a href="/property-management/" class="mobile-item" onclick="toggleMenu()">Property Owners</a>
            <a href="/guides/" class="mobile-item" onclick="toggleMenu()">Guides</a>
            <a href="#contact" class="mobile-item" onclick="toggleMenu()">Contact</a>

            <button class="btn" style="width:100%;margin-top:16px"
                onclick="window.open('https://book.seascape-vacations.com', '_blank'); toggleMenu()">Book
                Direct</button>
        </div>
    </nav>

    <!-- Quick View Modal -->
    <div class="quick-view-overlay" id="quick-view-overlay" onclick="closeQuickView(event)">
        <div class="quick-view-modal" onclick="event.stopPropagation()">
            <button class="quick-view-close" type="button" aria-label="Close quick view" onclick="closeQuickView(event)">{{ uiIcon('close', 'ui-icon--control') }}</button>
            <div class="quick-view-gallery" id="quick-view-gallery">
                <img id="quick-view-img" src="" alt="Property photo">
                <button class="quick-view-nav prev" onclick="quickViewNav(-1)">‹</button>
                <button class="quick-view-nav next" onclick="quickViewNav(1)">›</button>
                <div class="quick-view-dots" id="quick-view-dots"></div>
            </div>
            <div class="quick-view-content">
                <h2 class="quick-view-title" id="quick-view-title"></h2>
                <p class="quick-view-location" id="quick-view-location"></p>
                <div class="quick-view-rating">
                    {{ uiIcon('star', 'ui-icon--star') }}
                    <span id="quick-view-rating">5.0</span>
                </div>
                <div class="quick-view-highlights" id="quick-view-highlights"></div>
                <div class="quick-view-actions">
                    <button class="btn btn-gold" onclick="openQuickViewProperty()">View Full Details</button>
                    <button class="btn" onclick="closeQuickView(event)">Close</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Mobile CTA Bar (visible on mobile only) -->
    <div class="mobile-cta-bar">
        <div class="cta-info">
            <div class="cta-title">Book Direct & Save</div>
            <div class="cta-subtitle">Best rates guaranteed</div>
        </div>
        <button class="btn btn-gold" onclick="window.open('https://book.seascape-vacations.com', '_blank')">Book Now</button>
    </div>

    <!-- JSON-LD Structured Data for SEO -->
    <script type="application/ld+json">
    {
        "@context": "https://schema.org",
        "@type": "VacationRental",
        "name": "Seascape Vacations",
        "description": "Luxury vacation rentals in Anna Maria Island, Bradenton, and Sarasota Florida",
        "url": "https://seascape-vacations.com",
        "telephone": "+1-941-704-8545",
        "address": {
            "@type": "PostalAddress",
            "addressLocality": "Bradenton",
            "addressRegion": "FL",
            "addressCountry": "US",
            "postalCode": "34209"
        },
        "geo": {
            "@type": "GeoCoordinates",
            "latitude": "27.4989",
            "longitude": "-82.5748"
        },
        "aggregateRating": {
            "@type": "AggregateRating",
            "ratingValue": "5.0",
            "reviewCount": "500"
        },
        "priceRange": "$$$"
    }
    </script>

    <!-- HOME PAGE -->
    <div id="page-home" class="page active">
        <!-- Hero -->
        <section class="hero">
            <div class="hero-bg">
            </div>
            <div class="hero-overlay"></div>
            <div class="hero-content">
                <div class="hero-badge">
                    <span class="stars">{{ uiIcon('star', 'ui-icon--star') }}{{ uiIcon('star', 'ui-icon--star') }}{{ uiIcon('star', 'ui-icon--star') }}{{ uiIcon('star', 'ui-icon--star') }}{{ uiIcon('star', 'ui-icon--star') }}</span>
                    <span>Rated Excellent by 500+ Guests</span>
                </div>
                <h1 class="hero-title">
                    Your Gulf Coast Paradise Awaits
                    <span>ANNA MARIA ISLAND • BRADENTON • SARASOTA</span>
                </h1>
                <p class="hero-subtitle">Handpicked luxury vacation rentals with private pools, waterfront views, and
                    five-star service.</p>
                <div style="display:flex;gap:16px;justify-content:center;flex-wrap:wrap">
                    <a class="btn btn-gold" href="/properties/">Explore Properties</a>
                    <a class="btn btn-outline" href="#contact">Contact Us</a>
                </div>
            </div>
            <div class="search-container">
                <div class="search-box">
                    <div class="search-header">
                        <p>Find your perfect getaway</p>
                    </div>
                    <div class="search-fields">
                        <div class="search-field">
                            <label for="location-select" class="search-label">Location</label>
                            <select id="location-select" style="border:none; background:transparent; font-size:15px; font-weight:500; color:var(--stone); width:100%; font-family:inherit; cursor:pointer; padding:0;">
                                <option value="ami">Anna Maria Area</option>
                                <option value="sarasota">Sarasota</option>
                            </select>
                        </div>

                        <button class="btn" onclick="handleSearch()"
                            style="height:100%;border-radius:12px">Search</button>
                    </div>
                </div>
            </div>
        </section>

    <script>
        function handleSearch() {
            const location = document.getElementById('location-select').value;
            window.location.href = location === 'sarasota'
                ? '/properties/?area=sarasota'
                : '/properties/?area=anna-maria-island';
        }
    </script>

        <!-- Stats Bar -->
        <div style="padding-top:100px">
            <div class="container">
                <div class="stats-bar">
                    <div class="stat-item">
                        <div class="stat-value">4.98</div>
                        <div class="stat-label">Airbnb Rating</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value">650+</div>
                        <div class="stat-label">5-Star Reviews</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value">10-15%</div>
                        <div class="stat-label">Book Direct Savings</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value">24/7</div>
                        <div class="stat-label">Local Support</div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Welcome Section -->
        <section class="section" id="welcome" style="padding-top:40px">
            <div class="container">
                <div class="section-header">
                    <p class="section-tag">Welcome to Seascape</p>
                    <h2 class="section-title">Exceptional Service & Exceptional Fun</h2>
                    <p class="section-subtitle">Our guests book vacations confidently knowing they're going to find
                        exactly what they're looking for without any surprises. Even better, if anything is off about
                        your stay, we'll make it right. That's the Seascape promise.</p>
                </div>
            </div>
        </section>

        <!-- Featured Properties -->
        <section class="section" style="padding-top:0">
            <div class="container">
                <div class="property-section-header">
                    <div>
                        <p class="section-tag" style="text-align:left">Our Collection</p>
                        <h2 class="section-title" style="margin-bottom:0">Featured Properties</h2>
                    </div>
                    <a href="/properties/" class="property-section-cta">View All Properties →</a>
                </div>
                <div class="property-grid">
                    {% for property in properties.slice(0, 3) %}
                    <a class="prop-card prop-card-link scroll-reveal revealed" href="/properties/{{ property.slug }}/">
                        <div class="prop-card-shadow"></div>
                        <img
                            loading="lazy"
                            decoding="async"
                            src="{{ property.image | imgProxy(800) }}"
                            srcset="{{ property.image | imgProxy(400) }} 400w, {{ property.image | imgProxy(800) }} 800w, {{ property.image | imgProxy(1200) }} 1200w"
                            sizes="(min-width: 1100px) 50vw, 100vw"
                            onerror="this.onerror=null;this.src='/images/seascape-og-default.jpg';this.srcset='';"
                            alt="{{ property.name }}">
                        <div class="prop-card-overlay"></div>
                        <div class="prop-card-content">
                            <div class="prop-card-top">
                                <div>
                                    <h3 class="prop-title">{{ property.name }}</h3>
                                    <p class="prop-location">{{ property.city }} · {{ property.specs }}</p>
                                </div>
                                <div class="prop-badge">{{ uiIcon('star', 'ui-icon--star') }} {{ property.rating }}</div>
                            </div>
                            <div>
                                <p class="prop-desc-snippet">
                                    {{ property.description }}
                                </p>
                                <div class="prop-highlights">
                                    {% for highlight in property.highlights %}
                                    <div class="prop-highlight"><span class="prop-dot"></span>{{ highlight }}</div>
                                    {% endfor %}
                                </div>
                                <div class="prop-bottom">
                                    <span class="prop-btn">View Details →</span>
                                </div>
                            </div>
                        </div>
                    </a>
                    {% endfor %}
                </div>
            </div>
        </section>

        <!-- Why Book Direct -->
        <section class="section" style="background:var(--cream-dark)">
            <div class="container">
                <div class="section-header">
                    <p class="section-tag">Why Book Direct</p>
                    <h2 class="section-title">The Seascape Difference</h2>
                </div>
                <div class="feature-grid">
                    <div class="feature-card">
                        <div class="feature-icon"><svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="M12 2v20M17 5H9.5a3.5 3.5 0 0 0 0 7h5a3.5 3.5 0 0 1 0 7H6"/></svg></div>
                        <h3 class="feature-title">Best Price Guaranteed</h3>
                        <p class="feature-desc">Book direct and save up to 20%. No hidden OTA fees or service charges.
                        </p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon"><svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path d="m3 9 9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg></div>
                        <h3 class="feature-title">Local Hosts Who Care</h3>
                        <p class="feature-desc">We live here and love it. Get real insider tips and personalized
                            recommendations.</p>
                    </div>
                    <div class="feature-card">
                        <div class="feature-icon"><svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><polygon points="12 2 15.09 8.26 22 9.27 17 14.14 18.18 21.02 12 17.77 5.82 21.02 7 14.14 2 9.27 8.91 8.26 12 2"/></svg></div>
                        <h3 class="feature-title">5-Star Service Always</h3>
                        <p class="feature-desc">Immaculate homes, thoughtful touches, and 24/7 support for peace of
                            mind.</p>
                    </div>
                </div>
            </div>
        </section>

        <!-- Reviews -->
        <section class="section">
            <div class="container">
                <div class="section-header">
                    <p class="section-tag">Guest Reviews</p>
                    <h2 class="section-title">What Our Guests Say</h2>
                </div>
                <div class="review-grid">
                    <div class="review-card">
                        <div class="review-stars">{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}</div>
                        <p class="review-text">"This home was by far the best we have ever rented. We saw Manatees and
                            Dolphins right from the back porch!! The home was absolutely beautiful, and decorated to a
                            T!"</p>
                        <div class="review-author">
                            <div class="review-avatar">K</div>
                            <div>
                                <div class="review-name">Kate O'Hara</div>
                                <div class="review-property">Dockside Dreams</div>
                            </div>
                        </div>
                    </div>
                    <div class="review-card">
                        <div class="review-stars">{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}</div>
                        <p class="review-text">"Hands down the best rental we've ever experienced. The place was
                            spotlessly clean and the hosts responded to every question immediately!"</p>
                        <div class="review-author">
                            <div class="review-avatar">K</div>
                            <div>
                                <div class="review-name">Kurt Jones</div>
                                <div class="review-property">Dockside Dreams</div>
                            </div>
                        </div>
                    </div>
                    <div class="review-card">
                        <div class="review-stars">{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}{{ uiIcon('star', 'ui-icon--review-star') }}</div>
                        <p class="review-text">"Absolutely perfect for our family vacation! The property is stunning and
                            had everything we needed. Can't wait to come back!"</p>
                        <div class="review-author">
                            <div class="review-avatar">F</div>
                            <div>
                                <div class="review-name">Farzad Alizadeh</div>
                                <div class="review-property">Sarasota Luxe</div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </section>

        <!-- CTA Section -->
        <section class="cta-section">
            <div class="cta-bg"
                style="background-image:url('/images/sarasota-sunset-hero.jpg')">
            </div>
            <div class="cta-overlay"></div>
            <div class="cta-content">
                <p style="font-size:12px;letter-spacing:0.2em;text-transform:uppercase;opacity:0.8;margin-bottom:16px">
                    For Property Owners</p>
                <h2 class="cta-title">Partner With Seascape Vacations</h2>
                <p class="cta-text">Own a property in the Bradenton-Sarasota area? Join our portfolio and enjoy
                    exceptional returns with our full-service property management.</p>
                <div class="cta-buttons">
                    <a class="btn btn-white" href="/property-management/">Learn More</a>
                    <a href="tel:9417048545" class="btn btn-outline">Call (941) 704-8545</a>
                </div>
            </div>
        </section>

        <!-- Blog Preview -->
        <section class="section">
            <div class="container">
                <div class="property-section-header">
                    <div>
                        <p class="section-tag" style="text-align:left">Plan Your Stay</p>
                        <h2 class="section-title" style="margin-bottom:0">Start With The Right Guide</h2>
                    </div>
                    <a href="/guides/" style="color:var(--brand);font-weight:500;text-decoration:none">View all →</a>
                </div>
                <div class="blog-grid">
                    <a class="blog-card" href="/guides/bradenton-vs-sarasota/" style="display:block;color:inherit;text-decoration:none">
                        <div class="blog-card-img"><img
                                src="/images/sarasota-sunset-hero.jpg"
                                alt="Bradenton vs Sarasota comparison" width="400" height="250" loading="lazy"
                                decoding="async"></div>
                        <div class="blog-card-body">
                            <span class="blog-tag">City Comparison</span>
                            <h3 class="blog-card-title">Bradenton vs Sarasota</h3>
                            <p class="blog-card-excerpt">Use the high-intent comparison first if you are choosing between value, beaches, dining, and pace.</p>
                            <p class="blog-card-meta">Updated March 2026</p>
                        </div>
                    </a>
                    <a class="blog-card" href="/guides/anna-maria-island-vs-siesta-key/" style="display:block;color:inherit;text-decoration:none">
                        <div class="blog-card-img"><img
                                src="/images/siesta-key-intro.webp"
                                alt="Anna Maria Island vs Siesta Key comparison" width="400" height="250" loading="lazy"
                                decoding="async"></div>
                        <div class="blog-card-body">
                            <span class="blog-tag">Beach Comparison</span>
                            <h3 class="blog-card-title">Anna Maria Island vs Siesta Key</h3>
                            <p class="blog-card-excerpt">See which island fits your trip before you lock in the wrong vibe for a week.</p>
                            <p class="blog-card-meta">Updated March 2026</p>
                        </div>
                    </a>
                    <a class="blog-card" href="/guides/anna-maria-island-area-guide/" style="display:block;color:inherit;text-decoration:none">
                        <div class="blog-card-img"><img
                                src="/images/ami-hero.webp"
                                alt="Anna Maria Island area guide" width="400" height="250" loading="lazy"
                                decoding="async"></div>
                        <div class="blog-card-body">
                            <span class="blog-tag">Destination Guide</span>
                            <h3 class="blog-card-title">Anna Maria Island Area Guide</h3>
                            <p class="blog-card-excerpt">Get the logistics, neighborhoods, and beach access context before you book.</p>
                            <p class="blog-card-meta">Local planning guide</p>
                        </div>
                    </a>
                </div>
            </div>
        </section>
    </div>

    <!-- FOOTER -->
    <footer class="footer" id="contact">
        <div class="container">
            <div class="footer-grid" style="grid-template-columns: 2fr 1fr 1fr 1fr 1fr;">
                <div>
                    <div class="footer-brand">
                        <svg viewBox="0 0 100 100" width="44" height="44" fill="none" stroke="rgba(255,255,255,0.9)"
                            stroke-width="3" stroke-linecap="round" stroke-linejoin="round">
                            <path d="M50 12 L88 78 Q92 86 84 88 L16 88 Q8 86 12 78 Z" />
                            <path d="M20 72 Q32 50 48 56 Q62 62 72 52 Q78 46 82 48" stroke-width="3.5" />
                            <path d="M26 62 Q32 54 38 58" stroke-width="2.5" />
                            <path d="M30 70 Q36 62 42 66" stroke-width="2.5" />
                            <path d="M22 78 Q28 70 34 74" stroke-width="2.5" />
                        </svg>
                        <div class="footer-logo-text">Seascape Vacations</div>
                    </div>
                    <p class="footer-desc">Your premier destination for luxury vacation rentals in the
                        Sarasota-Bradenton area. Experience exceptional service and unforgettable stays.</p>
                    <div style="margin-top: 16px;">
                        <a href="tel:9417048545" class="footer-link"
                            style="display: flex; align-items: center; gap: 8px; margin-bottom: 8px;">{{ uiIcon('phone', 'ui-icon--footer') }} (941)
                            704-8545</a>
                        <a href="mailto:info@seascape-vacations.com" class="footer-link"
                            style="display: flex; align-items: center; gap: 8px; margin-bottom: 8px;">{{ uiIcon('email', 'ui-icon--footer') }}
                            info@seascape-vacations.com</a>
                        <span class="footer-link" style="display: flex; align-items: center; gap: 8px;">{{ uiIcon('location', 'ui-icon--footer') }}
                            Sarasota-Bradenton, FL</span>
                    </div>
                </div>
                <div>
                    <p class="footer-heading">Company</p>
                    <a class="footer-link" href="/about-us/">About Us</a>
                    <a class="footer-link" href="/properties/">Our Properties</a>
                    <a class="footer-link" href="/property-management/">For Owners</a>
                    <a class="footer-link" href="#contact">Contact</a>
                </div>
                <div>
                    <p class="footer-heading">Support</p>
                    <span class="footer-link" onclick="openLegalModal('support')">FAQs</span>
                    <span class="footer-link" onclick="openLegalModal('terms')">Booking Policy</span>
                    <span class="footer-link" onclick="openLegalModal('terms')">Cancellation</span>
                    <span class="footer-link" onclick="openLegalModal('support')">Guest Guide</span>
                </div>
                <div>
                    <p class="footer-heading">Legal</p>
                    <span class="footer-link" onclick="openLegalModal('privacy')">Privacy Policy</span>
                    <span class="footer-link" onclick="openLegalModal('terms')">Terms of Service</span>
                    <span class="footer-link" onclick="openLegalModal('cookies')">Cookie Policy</span>
                </div>
                <div>
                    <p class="footer-heading">Follow Us</p>
                    <div class="footer-social">
                        <a href="{{ site.socialLinks.facebook }}" target="_blank" rel="noopener noreferrer"
                            class="footer-social-link" title="Facebook">FB</a>
                        <a href="{{ site.socialLinks.instagram }}" target="_blank" rel="noopener noreferrer"
                            class="footer-social-link" title="Instagram">IG</a>
                        {% if site.businessProfiles and site.businessProfiles.googleBusinessProfile %}
                        <a href="{{ site.businessProfiles.googleBusinessProfile }}" target="_blank" rel="noopener noreferrer"
                            class="footer-social-link" title="Google Business Profile">G</a>
                        {% endif %}
                    </div>
                </div>
            </div>
            <div class="footer-bottom">© 2026 Seascape Vacations. All rights reserved.</div>
        </div>
    </footer>

    <script>
        function createIconMarkup(name, className = '') {
            const icons = {
                menu: '<path d="M4 7h16"/><path d="M4 12h16"/><path d="M4 17h16"/>',
                close: '<path d="M6 6 18 18"/><path d="m6 18 12-12"/>',
                check: '<path d="m5 12 4 4L19 6"/>',
                info: '<circle cx="12" cy="12" r="9"/><path d="M12 11v5"/><path d="M12 8h.01"/>',
                heart: '<path d="m12 20-1.45-1.32C5.4 14.03 2 10.97 2 7.25 2 4.42 4.24 2.2 7.05 2.2c1.6 0 3.13.75 4.1 1.94.97-1.19 2.5-1.94 4.1-1.94C18.76 2.2 21 4.42 21 7.25c0 3.72-3.4 6.78-8.55 11.43L12 20Z"/>',
                star: '<path d="m12 3.5 2.78 5.63 6.22.91-4.5 4.39 1.06 6.19L12 17.7l-5.56 2.92 1.06-6.19L3 10.04l6.22-.91L12 3.5Z"/>'
            };
            const extraClass = className ? ` ${className}` : '';
            return `<span class="ui-icon${extraClass}" aria-hidden="true"><svg viewBox="0 0 24 24">${icons[name] || icons.info}</svg></span>`;
        }

        // Mobile Menu
        function toggleMenu() {
            const menu = document.getElementById('mobileMenu');
            const btn = document.querySelector('.mobile-btn');
            menu.classList.toggle('active');
            const isExpanded = menu.classList.contains('active');
            btn.innerHTML = isExpanded
                ? createIconMarkup('close', 'ui-icon--mobile-toggle')
                : createIconMarkup('menu', 'ui-icon--mobile-toggle');
            btn.setAttribute('aria-label', isExpanded ? 'Close menu' : 'Open menu');
            btn.setAttribute('aria-expanded', isExpanded ? 'true' : 'false');
        }

        // Scroll Effect for Nav
        window.addEventListener('scroll', () => {
            const nav = document.getElementById('mainNav');
            if (window.scrollY > 50) {
                nav.classList.add('scrolled');
            } else {
                nav.classList.remove('scrolled');
            }
        });

        // ===== SCROLL REVEAL ANIMATIONS =====
        // Use Intersection Observer for performance
        const revealObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('revealed');
                    // Optional: stop observing after revealed for performance
                    // revealObserver.unobserve(entry.target);
                }
            });
        }, {
            threshold: 0.15,
            rootMargin: '0px 0px -50px 0px'
        });

        function initScrollReveal() {
            // Add scroll-reveal class to elements
            const selectors = [
                '.section-header',
                '.stats-bar',
                '.feature-card',
                '.prop-card',
                '.review-card',
                '.blog-card',
                '.contact-card',
                '.cta-content'
            ];

            selectors.forEach(selector => {
                document.querySelectorAll(selector).forEach(el => {
                    el.classList.add('scroll-reveal');
                    revealObserver.observe(el);
                });
            });

            // Stagger reveal for grids
            document.querySelectorAll('.feature-grid, .review-grid, .blog-grid').forEach(grid => {
                grid.classList.add('stagger-reveal');
                revealObserver.observe(grid);
            });
        }

        function checkScrollReveal() {
            document.querySelectorAll('.scroll-reveal').forEach(el => {
                const rect = el.getBoundingClientRect();
                if (rect.top < window.innerHeight * 0.85) {
                    el.classList.add('revealed');
                }
            });
            document.querySelectorAll('.stagger-reveal').forEach(el => {
                const rect = el.getBoundingClientRect();
                if (rect.top < window.innerHeight * 0.85) {
                    el.classList.add('revealed');
                }
            });
        }

        // ===== ANIMATED COUNTERS =====
        function animateCounter(element, target, duration = 2000) {
            const start = 0;
            const startTime = performance.now();
            const isDecimal = target.includes('.');
            const numericTarget = parseFloat(target);
            const suffix = target.replace(/[\d.]/g, '');

            // If target is not a number, just display it directly (e.g., "Top 1%", "24/7")
            if (isNaN(numericTarget)) {
                element.textContent = target;
                return;
            }

            function update(currentTime) {
                const elapsed = currentTime - startTime;
                const progress = Math.min(elapsed / duration, 1);
                // Ease out cubic
                const eased = 1 - Math.pow(1 - progress, 3);
                const current = start + (numericTarget - start) * eased;

                if (isDecimal) {
                    element.textContent = current.toFixed(1) + suffix;
                } else {
                    element.textContent = Math.round(current) + suffix;
                }

                if (progress < 1) {
                    requestAnimationFrame(update);
                }
            }

            requestAnimationFrame(update);
        }

        const counterObserver = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting && !entry.target.dataset.counted) {
                    entry.target.dataset.counted = 'true';
                    const target = entry.target.dataset.target || entry.target.textContent;
                    animateCounter(entry.target, target);
                }
            });
        }, { threshold: 0.5 });

        function initCounters() {
            document.querySelectorAll('[data-animate-counter="true"]').forEach(stat => {
                const text = stat.textContent.trim();
                stat.dataset.target = text;
                stat.textContent = '0';
                counterObserver.observe(stat);
            });
        }

        // ===== FLOATING PARTICLES =====
        function createParticles() {
            const heroes = document.querySelectorAll('.hero, .page-hero');

            heroes.forEach(hero => {
                // Check if particles already exist
                if (hero.querySelector('.hero-particles')) return;

                const particleContainer = document.createElement('div');
                particleContainer.className = 'hero-particles';

                // Create 12 particles for subtle effect
                for (let i = 0; i < 12; i++) {
                    const particle = document.createElement('div');
                    particle.className = 'hero-particle';
                    particle.style.left = Math.random() * 100 + '%';
                    particle.style.animationDelay = Math.random() * 15 + 's';
                    particle.style.animationDuration = (12 + Math.random() * 8) + 's';
                    particle.style.opacity = 0.2 + Math.random() * 0.3;
                    particle.style.width = (2 + Math.random() * 4) + 'px';
                    particle.style.height = particle.style.width;
                    particleContainer.appendChild(particle);
                }

                hero.appendChild(particleContainer);
            });
        }

        // ===== INTERACTIVE HOVER EFFECTS =====
        function initHoverEffects() {
            // Magnetic button effect for CTAs
            document.querySelectorAll('.btn').forEach(btn => {
                btn.addEventListener('mousemove', (e) => {
                    const rect = btn.getBoundingClientRect();
                    const x = e.clientX - rect.left - rect.width / 2;
                    const y = e.clientY - rect.top - rect.height / 2;
                    btn.style.transform = `translate(${x * 0.1}px, ${y * 0.1}px)`;
                });

                btn.addEventListener('mouseleave', () => {
                    btn.style.transform = '';
                });
            });

            // Parallax effect on property cards (subtle)
            document.querySelectorAll('.prop-card').forEach(card => {
                card.addEventListener('mousemove', (e) => {
                    const rect = card.getBoundingClientRect();
                    const x = (e.clientX - rect.left) / rect.width - 0.5;
                    const y = (e.clientY - rect.top) / rect.height - 0.5;

                    const img = card.querySelector('img');
                    if (img) {
                        img.style.transform = `scale(1.08) translate(${x * 10}px, ${y * 10}px)`;
                    }
                });

                card.addEventListener('mouseleave', () => {
                    const img = card.querySelector('img');
                    if (img) {
                        img.style.transform = '';
                    }
                });
            });
        }

        // ===== SMOOTH PARALLAX ON SCROLL =====
        let ticking = false;
        function handleParallax() {
            if (!ticking) {
                requestAnimationFrame(() => {
                    const scrolled = window.scrollY;

                    // Subtle parallax on hero backgrounds
                    document.querySelectorAll('.hero-bg, .page-hero-bg, .cta-bg').forEach(bg => {
                        const speed = 0.3;
                        bg.style.transform = `translateY(${scrolled * speed}px) scale(1.1)`;
                    });

                    ticking = false;
                });
                ticking = true;
            }
        }

        // ===== INITIALIZATION =====
        document.addEventListener('DOMContentLoaded', () => {
            // Check for reduced motion preference
            const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

            if (!prefersReducedMotion) {
                initScrollReveal();
                initCounters();
                createParticles();
                initHoverEffects();
                window.addEventListener('scroll', handleParallax, { passive: true });
            }

            // Initial check for elements in view
            setTimeout(checkScrollReveal, 200);
        });

        // Smooth scroll polyfill for older browsers
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth' });
                }
            });
        });




        // ===== IMAGE OPTIMIZATION ENGINE =====
        // Serve Hostaway listing media through Hostaway's own CDN for more reliable delivery.
        function optimizeImage(url, width = 400) {
            if (!url || typeof url !== 'string') return url;
            const cleanUrl = url.split('?')[0];
            if (cleanUrl.includes('bookingenginecdn.hostaway.com/')) {
                return `${cleanUrl}?width=${width}&quality=78&format=webp&v=2`;
            }
            return url;
        }

        // Transform all image URLs in property data for performance
        function optimizePropertyImages(data) {
            return data.map(p => ({
                ...p,
                id: String(p.id),
                image: optimizeImage(p.image, 400),
                images: (p.images || []).map(img => optimizeImage(img, 400))
            }));
        }

        function firstDefined(...values) {
            return values.find(value => value !== undefined && value !== null && value !== '');
        }

        function summarizeDescription(description, fallbackDescription = '') {
            if (typeof description !== 'string' || !description.trim()) {
                return fallbackDescription;
            }

            const firstParagraph = description
                .replace(/\r/g, '')
                .split('\n\n')
                .map(part => part.trim())
                .find(Boolean) || description.trim();

            if (firstParagraph.length <= 260) {
                return firstParagraph;
            }

            const firstSentence = firstParagraph.match(/^(.{0,260}?[.!?])(\s|$)/);
            if (firstSentence && firstSentence[1]) {
                return firstSentence[1].trim();
            }

            return `${firstParagraph.slice(0, 257).trim()}...`;
        }

        // ===== PROPERTY DATA ENGINE =====
        const propertySeed = {{ properties | json | safe }};

        function deriveTags(amenities = []) {
            const tags = new Set();
            const list = amenities.map(a => String(a).toLowerCase());
            if (list.some(a => a.includes('waterfront'))) tags.add('Waterfront');
            if (list.some(a => a.includes('pool'))) tags.add('Private Pool');
            if (list.some(a => a.includes('pet'))) tags.add('Pet Friendly');
            if (list.some(a => a.includes('hot') || a.includes('spa'))) tags.add('Hot Tub');
            return tags.size ? Array.from(tags) : ['All Stays'];
        }
        const propertyData = optimizePropertyImages(
            (propertySeed || []).map(p => ({
                ...p,
                id: String(p.id || p.slug),
                slug: p.slug || "",
                title: p.title || p.name,
                location: p.location || p.city || "",
                guests: p.guests || 0,
                bedrooms: p.bedrooms || 0,
                bathrooms: p.bathrooms || 0,
                rating: p.rating || 5.0,
                image: p.heroImage || p.image || "",
                images: Array.isArray(p.gallery) && p.gallery.length ? p.gallery : (p.images || []),
                description: p.description ? summarizeDescription(p.description, "") : "",
                highlights: Array.isArray(p.highlights) ? p.highlights : [],
                amenities: Array.isArray(p.amenities) ? p.amenities : [],
                specs: p.specs || (String(p.bedrooms || 0) + " BR · " + String(p.bathrooms || 0) + " BA · Sleeps " + String(p.guests || 0)),
                tags: Array.isArray(p.tags) && p.tags.length ? p.tags : deriveTags(p.amenities || []),
                bookingUrl: p.bookingUrl || ""
            }))
        );

        function renderProperties(filter = 'All Stays') {
            const container = document.getElementById('property-grid');
            if (!container) return;

            container.innerHTML = '';

            const filtered = filter === 'All Stays'
                ? propertyData
                : propertyData.filter(p => p.tags.includes(filter));

            filtered.forEach(p => {
                const card = document.createElement('div');
                card.className = 'prop-card scroll-reveal revealed';
                card.onclick = () => showPropertyDetail(p.id);
                card.setAttribute('role', 'link');
                card.setAttribute('tabindex', '0');
                card.onkeydown = (event) => {
                    if (event.key === 'Enter' || event.key === ' ') {
                        event.preventDefault();
                        showPropertyDetail(p.id);
                    }
                };

                const primaryImage = Array.isArray(p.images) && p.images.length > 0 ? p.images[0] : p.image;
                const highlights = Array.isArray(p.highlights) ? p.highlights.slice(0, 3) : [];
                const description = p.description || '';
                card.innerHTML = `
                    <div class="prop-card-shadow"></div>
                    <img loading="lazy" decoding="async" src="${primaryImage}" alt="${p.title}">
                    <div class="prop-card-overlay"></div>
                    <div class="prop-card-content">
                        <div class="prop-card-top">
                            <div>
                                <h3 class="prop-title">${p.title}</h3>
                                <p class="prop-location">${p.location} · ${p.specs}</p>
                            </div>
                            <div class="prop-badge">${createIconMarkup('star', 'ui-icon--star')} ${p.rating}</div>
                        </div>
                        <div>
                            ${description ? `<p class="prop-desc-snippet">${description}</p>` : ''}
                            <div class="prop-highlights">
                                ${highlights.map(h => `<div class="prop-highlight"><span class="prop-dot"></span>${h}</div>`).join('')}
                            </div>
                            <div class="prop-bottom">
                                <span class="prop-btn" onclick="openQuickView(event, '${p.id}')" style="background:transparent;border:2px solid white;color:white;margin-right:8px;">Quick View</span>
                                <span class="prop-btn">View Details →</span>
                            </div>
                        </div>
                    </div>
                `;
                container.appendChild(card);
            });

            // Re-init observer for new elements
            document.querySelectorAll('.scroll-reveal').forEach(el => revealObserver.observe(el));
        }

        // Carousel state
        let currentCarouselImages = [];
        let currentCarouselIndex = 0;

        // Quick View Modal State
        let quickViewImages = [];
        let quickViewIndex = 0;
        let quickViewPropertyId = null;
        let touchStartX = 0;
        let touchEndX = 0;

        function openQuickView(event, propertyId) {
            event.stopPropagation();
            const p = propertyData.find(prop => prop.id === propertyId);
            if (!p) return;

            quickViewPropertyId = propertyId;
            quickViewImages = p.images || [p.image];
            quickViewIndex = 0;

            // Populate modal
            document.getElementById('quick-view-title').textContent = p.title;
            document.getElementById('quick-view-location').textContent = p.location + ', FL · ' + p.specs;
            document.getElementById('quick-view-rating').textContent = p.rating;
            
            // Highlights
            const highlightsContainer = document.getElementById('quick-view-highlights');
            highlightsContainer.innerHTML = (p.amenities || p.highlights || []).slice(0,5).map(h => 
                `<div class="quick-view-highlight">${createIconMarkup('check', 'ui-icon--highlight')} ${h}</div>`
            ).join('');

            // Gallery dots
            const dotsContainer = document.getElementById('quick-view-dots');
            dotsContainer.innerHTML = quickViewImages.slice(0,6).map((_, i) => 
                `<div class="quick-view-dot ${i === 0 ? 'active' : ''}" onclick="quickViewGoTo(${i})"></div>`
            ).join('');

            updateQuickViewImage();

            // Show modal
            document.getElementById('quick-view-overlay').classList.add('active');
            document.body.style.overflow = 'hidden';

            // Setup swipe gestures
            const gallery = document.getElementById('quick-view-gallery');
            gallery.addEventListener('touchstart', handleTouchStart, { passive: true });
            gallery.addEventListener('touchend', handleTouchEnd, { passive: true });
        }

        function closeQuickView(event) {
            if (event) event.stopPropagation();
            document.getElementById('quick-view-overlay').classList.remove('active');
            document.body.style.overflow = '';
        }

        function quickViewNav(direction) {
            quickViewIndex += direction;
            if (quickViewIndex < 0) quickViewIndex = quickViewImages.length - 1;
            if (quickViewIndex >= quickViewImages.length) quickViewIndex = 0;
            updateQuickViewImage();
        }

        function quickViewGoTo(index) {
            quickViewIndex = index;
            updateQuickViewImage();
        }

        function updateQuickViewImage() {
            document.getElementById('quick-view-img').src = quickViewImages[quickViewIndex];
            document.querySelectorAll('.quick-view-dot').forEach((dot, i) => {
                dot.classList.toggle('active', i === quickViewIndex);
            });
        }

        function openQuickViewProperty() {
            closeQuickView();
            if (quickViewPropertyId) showPropertyDetail(quickViewPropertyId);
        }

        // Swipe gesture handlers
        function handleTouchStart(e) {
            touchStartX = e.changedTouches[0].screenX;
        }

        function handleTouchEnd(e) {
            touchEndX = e.changedTouches[0].screenX;
            handleSwipe();
        }

        function handleSwipe() {
            const threshold = 50;
            if (touchEndX < touchStartX - threshold) quickViewNav(1);  // Swipe left = next
            if (touchEndX > touchStartX + threshold) quickViewNav(-1); // Swipe right = prev
        }

        // Toast Notification System
        function showToast(message, type = 'info') {
            let container = document.querySelector('.toast-container');
            if (!container) {
                container = document.createElement('div');
                container.className = 'toast-container';
                document.body.appendChild(container);
            }

            const icons = {
                success: createIconMarkup('check', 'ui-icon--toast'),
                error: createIconMarkup('close', 'ui-icon--toast'),
                info: createIconMarkup('info', 'ui-icon--toast')
            };
            const toast = document.createElement('div');
            toast.className = `toast ${type}`;
            toast.innerHTML = `
                <span class="toast-icon">${icons[type] || icons.info}</span>
                <span class="toast-message">${message}</span>
                <button class="toast-close" type="button" aria-label="Dismiss notification" onclick="this.parentElement.classList.add('hiding'); setTimeout(() => this.parentElement.remove(), 300)">${createIconMarkup('close', 'ui-icon--control')}</button>
            `;

            container.appendChild(toast);
            setTimeout(() => {
                toast.classList.add('hiding');
                setTimeout(() => toast.remove(), 300);
            }, 3000);
        }

        // Favorites functionality
        function toggleFavorite(event, propertyId) {
            event.stopPropagation();
            const btn = event.currentTarget;
            const heart = btn.querySelector('.heart');
            const favorites = JSON.parse(localStorage.getItem('seascape_favorites') || '[]');
            
            if (favorites.includes(propertyId)) {
                // Remove from favorites
                const index = favorites.indexOf(propertyId);
                favorites.splice(index, 1);
                btn.classList.remove('active');
                heart.innerHTML = createIconMarkup('heart', 'ui-icon--heart');
                showToast('Removed from favorites', 'info');
            } else {
                // Add to favorites
                favorites.push(propertyId);
                btn.classList.add('active');
                heart.innerHTML = createIconMarkup('heart', 'ui-icon--heart-filled');
                showToast('Added to favorites.', 'success');
            }
            
            localStorage.setItem('seascape_favorites', JSON.stringify(favorites));
        }

        // Check and restore favorite states on page load
        function initFavorites() {
            const favorites = JSON.parse(localStorage.getItem('seascape_favorites') || '[]');
            document.querySelectorAll('.favorite-btn').forEach(btn => {
                const propertyId = btn.getAttribute('onclick').match(/'([^']+)'/)?.[1];
                if (propertyId && favorites.includes(propertyId)) {
                    btn.classList.add('active');
                    btn.querySelector('.heart').innerHTML = createIconMarkup('heart', 'ui-icon--heart-filled');
                }
            });
        }

        function showPropertyDetail(id) {
            const p = propertyData.find(prop => prop.id === id);
            if (!p) return;
            window.location.href = p.slug ? `/properties/${p.slug}/` : '/properties/';
        }

        function changeCarouselImage(direction) {
            let newIndex = currentCarouselIndex + direction;
            if (newIndex < 0) newIndex = currentCarouselImages.length - 1;
            if (newIndex >= currentCarouselImages.length) newIndex = 0;
            setCarouselImage(newIndex);
        }

        function setCarouselImage(index) {
            currentCarouselIndex = index;
            document.getElementById('carousel-main-img').src = currentCarouselImages[index];
            document.getElementById('carousel-current').textContent = index + 1;

            // Update thumbnail active states
            document.querySelectorAll('.carousel-thumb').forEach((thumb, i) => {
                thumb.classList.toggle('active', i === index);
            });
        }

        // ===== LEGAL & SUPPORT MODALS =====
        const legalContent = {
            'privacy': {
                title: 'Privacy Policy',
                content: `<p>At Seascape Vacations, we prioritize your privacy. We collect only necessary information to process your bookings and improve your experience. We do not sell your data to third parties.</p><p><strong>Data Collection:</strong> We collect name, email, and phone number for booking purposes.</p><p><strong>Security:</strong> Your data is encrypted and stored securely.</p>`
            },
            'terms': {
                title: 'Terms of Service',
                content: `<p>By using this website, you agree to our terms. Bookings are subject to availability and confirmation.</p><p><strong>Cancellations:</strong> Please review the cancellation policy on the booking checkout page.</p><p><strong>Check-in/Out:</strong> Standard check-in is 4PM, check-out is 10AM.</p>`
            },
            'cookies': {
                title: 'Cookie Policy',
                content: `<p>We use cookies to enhance your browsing experience and analyze site traffic. By continuing to use our site, you consent to our use of cookies.</p>`
            },
            'support': {
                title: 'Support Center',
                content: `<p>Need help? Our local team is available 24/7.</p><p><strong>Phone:</strong> (941) 555-0123</p><p><strong>Email:</strong> <a href="mailto:support@seascape-vacations.com" style="color:var(--brand)">support@seascape-vacations.com</a></p><p>For urgent maintenance issues, please call the emergency line provided in your check-in instructions.</p>`
            }
        };

        window.openLegalModal = function (type) {
            const modal = document.getElementById('legal-modal');
            const data = legalContent[type];
            if (data) {
                document.getElementById('modal-title').textContent = data.title;
                document.getElementById('modal-body').innerHTML = data.content;
                modal.style.display = 'flex';
                setTimeout(() => modal.classList.add('active'), 10);
            }
        }

        window.closeLegalModal = function () {
            const modal = document.getElementById('legal-modal');
            modal.classList.remove('active');
            setTimeout(() => modal.style.display = 'none', 300);
        }

        // Initialize Engine
        document.addEventListener('DOMContentLoaded', () => {
            renderProperties();
            setTimeout(initFavorites, 1000); // Init favorites after properties load

            // Filter Logic
            document.querySelectorAll('.filter-btn').forEach(btn => {
                btn.addEventListener('click', (e) => {
                    document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
                    e.target.classList.add('active');
                    renderProperties(e.target.textContent);
                });
            });
        });
    </script>

    <!-- LEGAL MODAL -->
    <div id="legal-modal" class="modal-overlay" onclick="closeLegalModal()">
        <div class="modal-content" onclick="event.stopPropagation()">
            <span class="close-modal" onclick="closeLegalModal()">&times;</span>
            <h2 id="modal-title"
                style="margin-bottom:20px; font-family:'Playfair Display', serif; color:var(--brand-dark);"></h2>
            <div id="modal-body" style="line-height:1.8; color:var(--stone);"></div>
        </div>
    </div>

    <!-- EMAIL CAPTURE POPUP -->
    <div id="email-popup" class="email-popup-overlay" onclick="closeEmailPopup()">
        <div class="email-popup" onclick="event.stopPropagation()">
            <button class="email-popup-close" type="button" aria-label="Close offer popup" onclick="closeEmailPopup()">{{ uiIcon('close', 'ui-icon--control') }}</button>

            <!-- Form Content -->
            <div id="email-form-content">
                <span class="email-popup-badge">Exclusive Offer</span>
                <h3 class="email-popup-title">Get $50 Off Your Next Stay</h3>
                <p class="email-popup-subtitle">Join our list for exclusive deals, local tips, and priority access to
                    new properties. Book direct and save more.</p>

                <form class="email-popup-form" id="email-capture-form" onsubmit="handleEmailSubmit(event)">
                    <input type="text" name="name" class="email-popup-input" placeholder="Your first name" required>
                    <input type="email" name="email" class="email-popup-input" placeholder="Your email address"
                        required>
                    <button type="submit" class="email-popup-btn">Get My $50 Credit →</button>
                </form>

                <p class="email-popup-privacy">{{ uiIcon('secure', 'ui-icon--inline') }} No spam, ever. Unsubscribe anytime.</p>
            </div>

            <!-- Success Content -->
            <div id="email-success-content" class="email-popup-success">
                <div class="email-popup-success-icon">{{ uiIcon('check', 'ui-icon--success') }}</div>
                <h3 class="email-popup-title">You're In!</h3>
                <p class="email-popup-subtitle">Check your inbox for your exclusive $50 credit code. We'll only send
                    you the good stuff.</p>
                <button class="email-popup-btn" onclick="closeEmailPopup()" style="margin-top: 16px;">Start Browsing
                    Properties</button>
            </div>
        </div>
    </div>

    <script>
        // ===== EMAIL CAPTURE POPUP LOGIC =====
        const EMAIL_POPUP_KEY = 'seascape_email_popup_shown';
        const EMAIL_POPUP_DELAY = 30000; // 30 seconds
        let emailPopupReady = false; // Gate exit-intent until delay elapsed
        let emailPopupShown = false; // Prevent showing more than once per session

        function showEmailPopup() {
            if (emailPopupShown) return;

            // Don't show if already captured or dismissed recently
            const lastShown = localStorage.getItem(EMAIL_POPUP_KEY);
            if (lastShown === 'subscribed') return;
            if (lastShown) {
                const daysSince = (Date.now() - parseInt(lastShown)) / (1000 * 60 * 60 * 24);
                if (daysSince < 7) return; // Don't show for 7 days after dismiss
            }

            emailPopupShown = true;
            const popup = document.getElementById('email-popup');
            popup.style.display = 'flex';
            setTimeout(() => popup.classList.add('active'), 10);
        }

        function closeEmailPopup() {
            const popup = document.getElementById('email-popup');
            popup.classList.remove('active');
            setTimeout(() => popup.style.display = 'none', 400);

            // Mark as shown for 7 days
            localStorage.setItem(EMAIL_POPUP_KEY, Date.now().toString());
        }

        function handleEmailSubmit(event) {
            event.preventDefault();
            const form = event.target;
            const formData = new FormData(form);
            const name = formData.get('name');
            const email = formData.get('email');

            // Mailchimp form URL (from your Mailchimp embedded form)
            const MAILCHIMP_URL = 'https://seascape-vacations.us6.list-manage.com/subscribe/post?u=48f234eebd9cb530fd2f217fe&id=95e5a594d1&f_id=008996e5f0';

            // Create form data for Mailchimp
            const mailchimpData = new FormData();
            mailchimpData.append('EMAIL', email);
            mailchimpData.append('FNAME', name);

            // Submit to Mailchimp (using no-cors mode for cross-origin)
            fetch(MAILCHIMP_URL, {
                method: 'POST',
                body: mailchimpData,
                mode: 'no-cors'
            }).catch(err => console.log('Mailchimp submission:', err));

            // Mark as subscribed permanently
            localStorage.setItem(EMAIL_POPUP_KEY, 'subscribed');

            // Show success message
            document.getElementById('email-form-content').style.display = 'none';
            document.getElementById('email-success-content').classList.add('show');

            console.log('Email submitted to Mailchimp:', { name, email });
        }

        // Trigger popup after 30s delay (only on homepage)
        setTimeout(() => {
            emailPopupReady = true;
            if (document.getElementById('page-home')?.classList.contains('active')) {
                showEmailPopup();
            }
        }, EMAIL_POPUP_DELAY);

        // Also trigger on exit intent (desktop only) — only after delay has elapsed
        document.addEventListener('mouseleave', (e) => {
            if (emailPopupReady && e.clientY <= 0) {
                showEmailPopup();
            }
        });

        // ===== OWNER LEAD FORM HANDLER =====
        function handleOwnerLead(event) {
            event.preventDefault();
            const form = event.target;
            const formData = new FormData(form);

            const leadData = {
                name: formData.get('owner_name'),
                phone: formData.get('owner_phone'),
                email: formData.get('owner_email'),
                property_address: formData.get('property_address'),
                timestamp: new Date().toISOString()
            };

            console.log('Owner lead captured:', leadData);

            // Show success state
            const submitBtn = form.querySelector('button[type="submit"]');
            const originalText = submitBtn.textContent;
            submitBtn.textContent = 'Request Sent';
            submitBtn.style.background = 'var(--gold)';
            submitBtn.disabled = true;

            // Reset form
            form.reset();

            // Reset button after 3 seconds
            setTimeout(() => {
                submitBtn.textContent = originalText;
                submitBtn.style.background = '';
                submitBtn.disabled = false;
            }, 3000);

            // In production, send to email service or CRM here:
            // Example: emailjs.send('service_id', 'owner_template_id', leadData);
        }

        // ===== REVENUE CALCULATOR =====
        // Toggle amenity selection
        document.querySelectorAll('.calc-amenity-toggle').forEach(toggle => {
            toggle.addEventListener('click', function () {
                const checkbox = this.querySelector('input[type="checkbox"]');
                checkbox.checked = !checkbox.checked;
                this.style.background = checkbox.checked ? 'white' : 'var(--cream)';
                this.style.borderColor = checkbox.checked ? 'var(--brand)' : 'transparent';
                const checkboxSpan = this.querySelector('.calc-checkbox');
                if (checkbox.checked) {
                    checkboxSpan.style.background = 'var(--brand)';
                    checkboxSpan.style.borderColor = 'var(--brand)';
                    checkboxSpan.innerHTML = createIconMarkup('check', 'ui-icon--checkbox');
                    checkboxSpan.style.color = 'white';
                } else {
                    checkboxSpan.style.background = '';
                    checkboxSpan.style.borderColor = '#ccc';
                    checkboxSpan.innerHTML = '';
                }
            });
        });

        // Revenue data based on market research
        const calcBaseRevenue = {
            'anna-maria': { 1: 65000, 2: 95000, 3: 145000, 4: 190000, 5: 240000 },
            'holmes-beach': { 1: 60000, 2: 88000, 3: 135000, 4: 175000, 5: 220000 },
            'bradenton-beach': { 1: 55000, 2: 82000, 3: 125000, 4: 165000, 5: 205000 },
            'bradenton': { 1: 45000, 2: 68000, 3: 100000, 4: 135000, 5: 170000 },
            'sarasota': { 1: 52000, 2: 78000, 3: 115000, 4: 155000, 5: 195000 },
            'siesta-key': { 1: 62000, 2: 92000, 3: 140000, 4: 185000, 5: 235000 }
        };

        const calcPropertyMultipliers = { 'house': 1.0, 'condo': 0.85, 'cottage': 0.92, 'townhouse': 0.90 };
        const calcAmenityPremiums = { 'pool': 0.22, 'waterfront': 0.30, 'hottub': 0.08, 'dock': 0.18, 'pets': 0.06, 'beach': 0.12 };
        const calcOccupancyRates = { 'anna-maria': 82, 'holmes-beach': 80, 'bradenton-beach': 78, 'bradenton': 72, 'sarasota': 75, 'siesta-key': 79 };

        document.getElementById('owner-calculator-form')?.addEventListener('submit', function (e) {
            e.preventDefault();
            const location = document.getElementById('calc-location').value;
            const propertyType = document.getElementById('calc-property-type').value;
            const bedrooms = parseInt(document.getElementById('calc-bedrooms').value);
            const selectedAmenities = document.querySelectorAll('.calc-amenity-toggle input:checked');

            if (!location || !bedrooms || !propertyType) {
                alert('Please fill in all required fields');
                return;
            }

            let revenue = calcBaseRevenue[location][bedrooms] || 100000;
            revenue = revenue * (calcPropertyMultipliers[propertyType] || 1);

            let amenityBoost = 0;
            selectedAmenities.forEach(input => { amenityBoost += calcAmenityPremiums[input.value] || 0; });
            amenityBoost = Math.min(amenityBoost, 0.60);
            revenue = revenue * (1 + amenityBoost);
            revenue = Math.round(revenue / 1000) * 1000;

            const lowRange = Math.round(revenue * 0.88 / 1000) * 1000;
            const highRange = Math.round(revenue * 1.12 / 1000) * 1000;
            const baseOccupancy = calcOccupancyRates[location] || 75;
            const occupancy = Math.min(baseOccupancy + selectedAmenities.length, 92);
            const avgNightly = Math.round(revenue / 365 / (occupancy / 100));
            const netIncome = Math.round(revenue * 0.55);

            document.getElementById('calc-revenue-amount').textContent = '$' + revenue.toLocaleString();
            document.getElementById('calc-revenue-range').textContent = `Range: $${lowRange.toLocaleString()} - $${highRange.toLocaleString()}/year`;
            document.getElementById('calc-avg-nightly').textContent = '$' + avgNightly.toLocaleString();
            document.getElementById('calc-occupancy').textContent = occupancy + '%';
            document.getElementById('calc-net-income').textContent = '$' + netIncome.toLocaleString();

            document.getElementById('calc-results').style.display = 'block';
            document.getElementById('calc-results').scrollIntoView({ behavior: 'smooth' });
        });

        document.getElementById('calc-lead-form')?.addEventListener('submit', function (e) {
            e.preventDefault();
            const email = this.querySelector('input[type="email"]').value;
            console.log('Calculator lead captured:', email);

            const btn = this.querySelector('button[type="submit"]');
            btn.textContent = 'Sent!';
            btn.style.background = 'var(--brand)';
            btn.style.color = 'white';
            this.reset();

            setTimeout(() => {
                btn.textContent = 'Get Report';
                btn.style.background = 'var(--gold)';
                btn.style.color = 'var(--stone)';
            }, 3000);
        });
    </script>

</body>

</html>
