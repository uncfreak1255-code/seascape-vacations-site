# Cursor Bugbot baseline canary

This temporary document verifies that the repository's already-enabled Cursor
Bugbot runs on a fresh, non-draft pull request. It does not change site source,
build behavior, deployment configuration, permissions, or runtime state.

Canary phase: first head.
