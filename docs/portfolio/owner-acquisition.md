# Owner Acquisition

| Winner URL | Aliases / retired routes | Feeder pages | Money destination | Primary CTA | Tracked event | Success metric | Schema expectation | Sitemap state |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| `/property-management/vacation-rental-management-anna-maria-island/` | no alias family should be created without an evidence-driven reason | `/property-management/`, `/guides/florida-gulf-coast-vacation-rental-market-report-2026/`, owner comparison or economics pages | this page is the AMI owner money destination | `Request Your Revenue Review` | `owner_primary_cta_click` | 7-day `owner_form_submits > 0`; if sessions `>= 20` and CTA clicks stay at `0`, treat as CRO failure | `Service` + `Article` + `FAQPage` + `BreadcrumbList` | canonical page stays in sitemap |
| `/property-management/vacation-rental-management-bradenton/` | no alias family should be created without an evidence-driven reason | `/property-management/`, `/guides/florida-gulf-coast-vacation-rental-market-report-2026/`, owner economics pages | this page is the Bradenton owner money destination | `Request Your Revenue Review` | `owner_primary_cta_click` | 7-day `owner_form_submits > 0`; if sessions `>= 20` and CTA clicks stay at `0`, treat as CRO failure | `Service` + `Article` + `FAQPage` + `BreadcrumbList` | canonical page stays in sitemap |
| `/property-management/maximize-vacation-rental-income-florida/` | none documented | owner-education pages and the owner hub when the job is leak diagnosis, not location fit | this page is the owner-economics bridge page | `Request Your Revenue Review` | `owner_primary_cta_click` | 7-day `owner_form_submits > 0`; if CTA clicks appear but submits stay at `0`, audit form friction | `Service` + `Article` + `FAQPage` + `BreadcrumbList` | canonical page stays in sitemap |
| `/property-management/vacation-rental-management-fees-florida/` | none documented | owner hub, market report, income page, and owner-comparison pages | this page is a money page and should feed the revenue-review CTA | `Request Your Revenue Review` | `owner_primary_cta_click` | cluster enters rewrite only if combined owner impressions `>= 1000`, weighted CTR `< 0.20%`, weighted position `<= 10`, and owner submits stay at `0` | `Service` + `Article` + `FAQPage` + `BreadcrumbList` | canonical page stays in sitemap |
| `/property-management/vacation-rental-licensing-florida/` | none documented | owner hub, market report, and owner setup/compliance content | this page is a money page and should feed the revenue-review CTA | `Request Your Revenue Review` | `owner_primary_cta_click` | cluster enters rewrite only if combined owner impressions `>= 1000`, weighted CTR `< 0.20%`, weighted position `<= 10`, and owner submits stay at `0` | `Service` + `Article` + `FAQPage` + `BreadcrumbList` | canonical page stays in sitemap |
| `/property-management/vrbo-management-services-florida/` | none documented | owner hub, channel-mix pages, and owner economics content | this page is a money page and should feed the revenue-review CTA | `Request Your Revenue Review` | `owner_primary_cta_click` | cluster enters rewrite only if combined owner impressions `>= 1000`, weighted CTR `< 0.20%`, weighted position `<= 10`, and owner submits stay at `0` | `Service` + `Article` + `FAQPage` + `BreadcrumbList` | canonical page stays in sitemap |
